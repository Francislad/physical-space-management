// API Base URL
export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';

// User Roles
export const ROLES = {
  USER: 'user',
  ADMIN: 'admin',
};

// LocalStorage Keys
export const STORAGE_KEYS = {
  TOKEN: 'token',
  USER: 'user',
  CHECK_IN: 'checkIn',
};
